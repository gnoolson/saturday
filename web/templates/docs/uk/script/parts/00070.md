<h3 id="require">Функція require()</h3>

Функція `require()` шукає модулі в каталозі `./lualib`.
Шлях до каталогу можна змінити за допомогою параметра `gnoolson.saturday.script.lua.lib` у **Конфігурації Saturday**.

---